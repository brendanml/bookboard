{
  "name": "e2e",
  "version": "1.0.0",
  "description": "e2e testing with playwright",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test --project=chromium",
    "test:report": "npx playwright test --trace=on --project=chromium && npx playwright show-report",
    "test:ui": "npx playwright test --project=chromium --ui"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.51.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "@types/node": "^22.13.10"
  }
}
